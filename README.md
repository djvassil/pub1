# SCA Scanning Test Application

⚠️ **This repository is intentionally vulnerable.** It exists solely to test
Software Composition Analysis (SCA) scanning (Upwind). It deliberately declares
outdated third-party dependencies with known CVEs in `requirements.txt`.

Do **not** deploy this application or reuse its dependency versions anywhere real.
